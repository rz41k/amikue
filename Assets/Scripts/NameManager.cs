using System.Collections;

public class NameManager{
    //name管理用enum

    public enum GroundCheck {
        GroundCheck_L,
        GroundCheck_C,
        GroundCheck_R,
        GroundChecks,
    }

    public enum Bullet {
        StartingPointOfBullet,
    }

}
